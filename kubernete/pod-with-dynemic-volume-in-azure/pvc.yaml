# pvc-azurefile-dynamic.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azurefile-dynamic-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-csi
  resources:
    requests:
      storage: 5Gi
